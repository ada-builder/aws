export type VehicleFeed = {
  id: string;
  filename: string;
  host: string;
  name: string;
  password: string;
  port: string;
  provider: string;
  siteId: string;
  user: string;
  vehicleType: string;
};
